services:
  - type: web
    name: xponentialai-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn main:app --workers 1 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:10000
    envVars:
      - key: PORT
        value: 10000
    plan: free